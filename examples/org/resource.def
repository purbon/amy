{
  "name"    : "org",
  "title"   : "A single organization",
  "config" : {
      "get"     : { "url": "/orgs/:org", "desc": "Get\"s the given organization stored data" },
      "head"    : { "url": "/orgs/:org", "desc": "Return the server headers, without the body" },
      "options" : { "url": "/orgs/:org", "desc": "Get\"s the list of valid HTTP verbs" }

  }
}
